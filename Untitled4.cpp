#include<stdio.h>
#include<math.h>
int main ()

{
int a,b,c;
printf ("enter a ,b,c values");
scanf("%f %f %f ",&a,&b,&c);
D=b*b-4*a*c;
if (D>0)
{
	root 1 = -b+sqrt(D)/2*a;
	root 2 = -b-sqrt(D)/2*a;
}


	return 0;	
}
